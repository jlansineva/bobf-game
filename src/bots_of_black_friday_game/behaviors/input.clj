(ns bots-of-black-friday-game.behaviors.input)

(def input-entity
  {:id :input
   :keys {:left {:pressed false}
          :right {:pressed false}
          :up {:pressed false}
          :down {:pressed false}}})
